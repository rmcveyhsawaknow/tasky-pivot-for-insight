# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.env

# Go Build artifacts
tasky
oryxBuildBinary

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/


.idea
setup_test.sh
Makefile
deploy
test
tmp

# The terraform/.terraform directory should NOT be committed.
# This directory contains local state, provider plugins, and other environment-specific files.
# It should always be in your .gitignore.
terraform/.terraform
*.tfvars
terraform.tfstate
terraform.tfstate.backup
terraform.tfplan
*.tfplan

# DO commit .terraform.lock.hcl
# This file locks Terraform provider versions for reproducible builds across your team and CI/CD.
# Committing it ensures everyone uses the same provider versions, improving reliability and reducing "works on my

# Ignore downloaded utilities, such as kubeval and other tools used for validation or testing
scripts/utils/*